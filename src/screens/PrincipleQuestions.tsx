import { SafeAreaView } from 'react-native-safe-area-context';
import { Text } from 'react-native-ui-lib';

export default function PrincipleQuestions() {
    return (
        <SafeAreaView>
            <Text>Teste</Text>
        </SafeAreaView>
    );
}

const styles = {

};
